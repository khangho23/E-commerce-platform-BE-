'use strict'

const applyTo = Object.freeze({
    ALL: 'all',
    SPECIFIC_PRODUCT: 'specific_products'
})

const type = Object.freeze({
    FIXED_AMOUNT: 'fixed_amout'
})

module.exports = {
    applyTo,
    type
}